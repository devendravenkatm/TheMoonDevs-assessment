import React from 'react';

const ChainSelector = () => {
  return (
    <div className="chain-selector">
      {/* Render chain selection UI here */}
    </div>
  );
};

export default ChainSelector;
